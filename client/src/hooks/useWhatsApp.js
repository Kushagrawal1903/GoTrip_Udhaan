/**
 * useWhatsApp — DEPRECATED
 *
 * This hook previously managed the WhatsApp Cloud API integration
 * (modal state, phone number collection, backend API calls to Meta).
 *
 * REPLACED BY: WhatsApp Click-to-Chat sharing (inline in TripResult.jsx).
 * The new implementation uses https://wa.me/?text=<message> which requires
 * no backend, no API keys, no phone input, and works for all users.
 *
 * This file is retained for reference only. It is no longer imported anywhere.
 *
 * @deprecated Since May 2026 — use handleShareWhatsApp in TripResult.jsx
 */

// No exports — this file is intentionally inactive.
